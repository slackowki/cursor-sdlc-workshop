# Hat Photo

Add fancy hats to your photo. Click "Add a hat" to overlay one of six generated hats (top hat, bowler, fedora, beret, fascinator, wide-brimmed sun hat) on the person in the image.

## Run

```bash
npm install
npm run dev
```

Open http://localhost:5173/

## Build

```bash
npm run build
```
